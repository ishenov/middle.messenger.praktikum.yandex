<div class="login-page">
    <h1 class="login-page__title">Войти</h1>
    <form>
        {{> Input id="login" type="text" placeholder="Логин"}}
        {{> Input id="password" type="text" placeholder="Пароль"}}
        {{> Button id="add-question" text="Авторизоваться"}}
        {{> Button id="add-question" text="Зарегистрироваться" class="secondary"}}
    </form>
</div>
{{> Footer}}
