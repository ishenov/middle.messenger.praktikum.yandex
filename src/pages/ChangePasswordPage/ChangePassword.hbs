<div class="profile-page">
    <div class="sidebar">
        <button id="back-button" class="back-button">&#8592;</button>
    </div>

    <div class="profile-container">
        <div class="profile-avatar">
            <div class="avatar large"></div>
        </div>
        <div class="profile-name">{{firstName}}</div>

        <div class="profile-info">
            <div class="row">
                <div class="label">Старый пароль</div>
                <div class="value">
                    {{#if isEdit}}
                        {{> Input id="oldPassword" type="password" value="{{oldPassword}}"}}
                    {{else}}
                        {{email}}
                    {{/if}}
                </div>
            </div>

            <div class="row">
                <div class="label">Новый пароль</div>
                <div class="value">
                    {{#if isEdit}}
                        {{> Input id="newPassword" type="password" value="{{newPassword}}"}}
                    {{else}}
                        {{login}}
                    {{/if}}
                </div>
            </div>
        </div>

        <div class="profile-actions">
            {{#if isEdit}}
                {{> Button text="Сохранить" class="edit-link secondary"}}
            {{else}}
                {{> Button text="Изменить данные" class="edit-link secondary"}}
            {{/if}}
        </div>
    </div>
</div>
{{> Footer}}
